import { Zoom } from 'medium-zoom';
export declare class VuepressMediumZoom {
    instance: Zoom | null;
    update(selector?: string): void;
    updateDelay(selector?: string, delay?: number): void;
}
